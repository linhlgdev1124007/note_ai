import { redirect } from '@sveltejs/kit'; import { sql } from '$lib/server/db/postgres'; import type { PageServerLoad } from './$types';
export const load: PageServerLoad = async ({ locals }) => { if (!locals.user) throw redirect(303, '/login'); const notes = locals.user.role === 'ADMIN' ? await sql`SELECT id,title,deleted_at AS "deletedAt" FROM note WHERE deleted_at IS NOT NULL ORDER BY deleted_at DESC` : await sql`SELECT id,title,deleted_at AS "deletedAt" FROM note WHERE deleted_at IS NOT NULL AND owner_id=${locals.user.id} ORDER BY deleted_at DESC`; return { notes, isAdmin: locals.user.role === 'ADMIN' }; };
